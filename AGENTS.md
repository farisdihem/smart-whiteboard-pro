# AGENTS.md

## 🌟 نظرة عامة على المشروع (Project Overview)

تطبيق السبورة التعليمية التفاعلية والذكية للجزائر والعالم العربي (**"السبورة الذكية Pro - Whiteboard Pro AI"**).
تطبيق أحادي الصفحة (Single Page Application) مبني باستخدام **Vite + Vanilla JavaScript + HTML5 Canvas/SVG + Tailwind CSS**، ومغلف لسطح المكتب باستخدام **Tauri v1 (Rust)** وقاعدة بيانات سحابية **Firebase/Firestore**.

---

## 💻 الأوامر الأساسية للبناء والتشغيل (Key Commands)

```bash
# 1. تشغيل خادم التطوير المحلي (Dev Server على المنفذ 3000)
npm run dev

# 2. بناء ملفات الإنتاج وتدقيق الشيفرة (Production Build & Lint)
npm run build

# 3. بناء وتوليد أيقونات التطبيق النظيفة
npm run build:icon

# 4. تشغيل وضع سطح المكتب التطويري (يتطلب Rust + Tauri CLI)
npm run tauri dev

# 5. بناء الحزمة النهائية لسطح المكتب (Desktop Installer .exe / .msi)
npm run tauri build
```

---

## 🏗️ هيكلية المشروع والملفات الأساسية (Architecture)

- `app.js` — المحرك البرمجي الرئيسي والضخم (~16k سطر). يحتوي على محرك الرسم بالكانفاس، أدوات المواد العلمية التفاعلية، تكامل Firebase، التخزين المحلي IndexedDB، التراجع/الإعادة، تسجيل الدروس بالفيديو والصوت.
- `index.html` — نقطة الدخول الرئيسية، واستدعاء `app.js` و `style.css` والمكتبات.
- `style.css` — التنسيقات العامة والقواعد البصرية والتوافقية مع Tailwind CSS.
- `data/` — ملفات الوحدات الرياضية والعلمية والجغرافية:
  - `shapes3d_engine.js` — محرك إسقاط وحساب المجسمات الهندسية ثلاثية الأبعاد والإضاءة الشعاعية.
  - `biology_data.js` — أدوات ومجسمات الأحياء والبيولوجيا الخلوية والوراثية.
  - `geography_data.js` / `geography_geojson_data.js` / `geography_engine.js` — الخرائط التفاعلية والجيولوجيا والفلك.
- `src-tauri/` — إعدادات حزمة سطح المكتب Tauri (`tauri.conf.json`, `Cargo.toml`, الأيقونات `icons/`).
- `public/` — الموارد الثابتة (أيقونات، أصوات، خطوط).
- `package.json` — تعريف الاعتماديات وأوامر التشغيل.

---

## 🎨 التوجيهات الفنية للنموذج والذكاء الاصطناعي (Directives for AI Agents / KIM 2.6)

### 1. سياسة عدم التراجع والحفاظ على الاستقرار (Non-Destructive Modifications)
- **ممنوع كسر الكود**: المحرك الرئيسي مبني بـ Vanilla JS، أي تعديل على `app.js` يجب أن يحافظ على أسماء الدوال، معلمات الأحداث، وتكاملات الكانفاس الحالية.
- **التوافق التام مع اللغة العربية (RTL)**: جميع العناوين والتسميات التوضيحية داخل الرسومات والمجسمات باللغة العربية الفصحى السليمة وفق المنهاج التعليمي الجزائري والعربي.

### 2. معايير المجسمات ثلاثية الأبعاد (3D Inflated & Volumetric SVG Standard)
عند إنشاء أو تحديث أي رسم بيولوجي أو فيزيائي أو كيميائي أو رياضي، يتم تطبيق أسلوب **Soft Puffy 3D / Volumetric Studio Light**:
- استخدام التدرجات الشعاعية متغيرة المركز (`radialGradient` بـ `cx="35%" cy="30%" r="65%"`) والتدرجات الخطية متعددة الوقفات لإعطاء عمق انتفاخي ناعم.
- إضافة لمعان السطح العلوي (Specular Highlights) بحواف بيضاء ناعمة `opacity="0.6 - 0.8"`.
- إضافة ظلال ناعمة عبر فلاتر SVG الساقطة (`<filter id="..."><feDropShadow dx="3" dy="5" stdDeviation="4" flood-color="#0f172a" flood-opacity="0.25"/></filter>`).
- وضع التسميات العربية داخل شارات عائمة ثلاثية الأبعاد بخلفية شبه شفافة وحواف واضحة متباينة (`rx="6"` و `fill-opacity="0.96"`).
- تأمين معرفات الفلاتر والتدرجات لتكون فريدة وغير مكررة لمنع تداخل الألوان في الكانفاس.

### 3. تدقيق الأخطاء والبناء (Verification)
- بعد أي تعديل، يجب التحقق من صحة كود الجافاسكربت وسلامة قفل وسوم الـ SVG (`npm run build`).
- تجنب الاعتماديات الزائدة والملفات المؤقتة.
